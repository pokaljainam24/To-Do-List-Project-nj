<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Manager</title>
    <link rel="stylesheet" href="/public/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #000000;
            height: 100vh;
        }

        .navbar {
            background: #007bff;
        }

        .navbar-brand {
            color: #fff !important;
            font-weight: bold;
        }

        .task-container {
            background: #fff;
            height: 300px !important;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .list-group-item {
            border-radius: 5px;
            transition: all 0.3s;
        }

        .list-group-item:hover {
            background: #f1f1f1;
        }
    </style>
</head>

<body>

    <!-- Header -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="#">Task Manager</a>

            <a class="fs-3 d-flex flex-row justify-content-center align-items-center gap-2 text-light mx-5 text-decoration-none"
                href="/view"><span class="fs-5 fw-bold">View Data</span>
            </a>
        </div>
    </nav>

    <div class="container w-75 mt-5" style="margin-top: 200px !important;">
        <div class="task-container bg-dark text-light p-4">
            <h2 class="text-center mb-4">Task Manager</h2>

            <!-- Task Input Form -->
            <form action="/add" method="POST" class="mt-5 p-3 d-flex container">
                <input type="text" name="task" class="form-control me-2 p-3" placeholder="Enter a new task" required>
                <button type="submit" class="btn btn-primary w-25">Add Task</button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous">
    </script>
</body>

</html>